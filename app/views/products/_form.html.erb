<%= form_for @product do |p| %>
    <p>
      <%= p.label :name %>
      <%= p.text_area :name %>
    </p>
    <p>
      <%= p.label :price %>
      <%= p.number_field :price %>
    </p>
    <p>
      <%= p.submit %>
    </p>
    <%= link_to 'Back', root_path %>
<% end %>